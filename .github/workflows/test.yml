name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          repositories: |
            vweevers/hallmark-lint-action
            vweevers/additional-tags-action
            vweevers/dotnet-bump@v1.3.0
      - run: ls ../vweevers
      - run: git -C ../vweevers/hallmark-lint-action log -n 1
      - run: git -C ../vweevers/additional-tags-action log -n 1
      - run: git -C ../vweevers/dotnet-bump log -n 1
      - run: npm it
